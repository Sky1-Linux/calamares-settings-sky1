# Display manager configuration
---

# Supported display managers (in order of preference)
displaymanagers:
    - sddm
    - gdm
    - lightdm

# Default desktop session
defaultDesktopEnvironment:
    executable: "startplasma-wayland"
    desktopFile: "plasma"
