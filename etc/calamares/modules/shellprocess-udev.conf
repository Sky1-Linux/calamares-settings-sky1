# Ensure udev has finished creating device nodes before GRUB runs
# Fixes: "grub-probe: error: failed to get canonical path of /dev/sdX"
---
dontChroot: true
timeout: 30

script:
    - command: "udevadm settle --timeout=10"
    - command: "partprobe 2>/dev/null || true"
    - command: "sleep 1"
