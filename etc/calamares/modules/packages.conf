# Package installation configuration
---
backend: apt

# Remove live-specific packages after installation
operations:
    - remove:
        - calamares
        - calamares-settings-sky1
        - live-boot
        - live-config
        - live-config-systemd
